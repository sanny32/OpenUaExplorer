# OpenUaExplorer
OpenUaExplorer is an open source OPC UA client

## Code style

Private member variables use a leading underscore, for example `_endpointSelectorWidget`.
The generated Qt Designer pointer is always named `ui` without a leading underscore.
