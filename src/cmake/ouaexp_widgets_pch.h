#ifndef OUAEXP_WIDGETS_PCH_H
#define OUAEXP_WIDGETS_PCH_H

// Precompiled header for the ouaexp libraries that build against Qt Widgets.
// It is the shared header plus the widget base class, kept apart so that
// ouaexp_core can use the shared one without pulling Qt Widgets in.

#include "ouaexp_pch.h"

#include <QWidget>

#endif // OUAEXP_WIDGETS_PCH_H
